
export const types = {
    TRIPLET: 'TRIPLET'
}

/*
function triplet (scale, rootNote, octave) {
    const scaleLength = scale.length
    const rootNoteIndex = scale.findIndex(note => note.name === rootNote.name)
    const chord = []


}
*/

export const chordNotes = {
    [types.TRIPLET]: ()=>{}
}
